# Dependencies
node_modules/

# Build output
dist/
*.tsbuildinfo

# Single-executable (Node SEA) build artifacts (#17)
dist-sea/
*.blob
*.exe

# Runtime data (SQLite DB + persisted TLS cert)
data/

# Portal build output (placeholder index.html is committed; everything else is built in #12)
portal/dist/*
!portal/dist/index.html

# Local config / secrets (never commit real secrets)
entra-local.config.json
.env
.env.local

# Test artifacts
coverage/

# Cross-platform MSAL compat smoke-test build artifacts (#13)
test/compat/dotnet/bin/
test/compat/dotnet/obj/
test/compat/python/.venv/
**/__pycache__/

# Editor / OS
.DS_Store
.vscode/
.idea/
